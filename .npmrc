# NPM Configuration for Monorepo
# Use legacy peer deps to handle React Native peer dependency conflicts
legacy-peer-deps=true

# Automatically install workspace protocol dependencies
link-workspace-packages=true

# Enable workspace protocol
workspace-protocol=*

# Use enhanced-resolve for better monorepo support
prefer-workspace-packages=true
